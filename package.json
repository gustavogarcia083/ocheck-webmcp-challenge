{
  "name": "ocheck-webmcp-challenge",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "OCHECK WebMCP Challenge Edition: the official, organizer-governed and sponsor-enabled integration layer for fragmented experiences.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "node --test",
    "check": "node --check app.js && node --check src/demo-data.js && node --check src/guide-store.js && node --check src/site-tools.js && node --check scripts/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
